module SumOfMultiples
  ( sumOfMultiples
  ) where

import Effect.Exception.Unsafe (unsafeThrow)

sumOfMultiples :: Array Int -> Int -> Int
sumOfMultiples = unsafeThrow "You need to implement this function."
